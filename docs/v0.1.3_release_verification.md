# v0.1.3 Release Verification

Release date: 2026-06-20

Package: `manifold-guard==0.1.3`

Tag: `v0.1.3`

## GitHub release path

- Release-prep commit: `d896b45`
- EXP22 closure commit: `a69fa92`
- Release tag build: passed
- TestPyPI trusted publish workflow: passed
- PyPI trusted publish workflow: passed

## Local release-prep validation

- `python -m pytest -q`: `187 passed`
- EXP22 challenge corpus: `18 / 18`
- Tag build artifact smoke install: passed
- TestPyPI clean install smoke: passed
- PyPI clean install smoke: passed

## Package-index verification

TestPyPI smoke:

- Fresh virtual environment created outside the repository path.
- Installed `manifold-guard==0.1.3` from TestPyPI with PyPI dependency fallback.
- Imported `mbt_ai_tools`.
- Confirmed package version `0.1.3`.
- Ran an offline EXP22-style comparative-binding smoke successfully.

PyPI smoke:

- Fresh virtual environment created outside the repository path.
- Installed `manifold-guard==0.1.3` from PyPI.
- Imported `mbt_ai_tools`.
- Confirmed package version `0.1.3`.
- Ran an offline EXP22-style temporal-order smoke successfully.

## Challenge-corpus status at release

EXP22 challenge corpus result at release: `18 / 18`.

This is a development challenge-corpus pass result, not a universal safety or
fact-checking claim. It records that the current offline reference-bound guards
pass the checked EXP22 seed cases at the time of release.

## Public install command

```bash
python -m pip install manifold-guard==0.1.3
```

Optional embedding mode remains opt-in:

```bash
python -m pip install "manifold-guard[embeddings]==0.1.3"
```

## Release workflow records

- Tag build: https://github.com/Martin123132/ManifoldGuard/actions/runs/27852560234
- TestPyPI publish: https://github.com/Martin123132/ManifoldGuard/actions/runs/27852581552
- PyPI publish: https://github.com/Martin123132/ManifoldGuard/actions/runs/27852676544
- PyPI package: https://pypi.org/project/manifold-guard/0.1.3/
- TestPyPI package: https://test.pypi.org/project/manifold-guard/0.1.3/

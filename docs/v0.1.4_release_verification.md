# ManifoldGuard v0.1.4 Release Verification

Release date: 2026-06-21

Package: `manifold-guard==0.1.4`

Release source commit: `4127466`

GitHub release: https://github.com/Martin123132/ManifoldGuard/releases/tag/v0.1.4

## Summary

Version `0.1.4` closes the EXP23 challenge seed at `18 / 18` while keeping the
default runtime path offline-first.

The release was published to:

- TestPyPI: `manifold-guard==0.1.4`
- PyPI: `manifold-guard==0.1.4`

## Local release checks

Canonical release check:

```bash
python scripts/release_check.py --output D:\Temp\manifoldguard-exp23\v0.1.4-release-evidence.json
```

Result:

- status: `ready`
- blockers: `none`
- evidence: `passed`

EXP23 completion evidence:

```bash
manifold-eval --corpus examples/exp23_challenge_corpus.jsonl --format json --output D:\Temp\manifoldguard-exp23\exp23-complete.json
```

Result:

- status: `passed`
- passed cases: `18 / 18`

## GitHub Actions

Release-prep CI:

- Offline regression tests: https://github.com/Martin123132/ManifoldGuard/actions/runs/27890107888
- Docs and manifest quality checks: https://github.com/Martin123132/ManifoldGuard/actions/runs/27890107899

Publishing workflows:

- TestPyPI publish: https://github.com/Martin123132/ManifoldGuard/actions/runs/27890166033
- PyPI publish: https://github.com/Martin123132/ManifoldGuard/actions/runs/27890226233

## Public install smoke checks

TestPyPI install smoke:

```bash
python -m pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ manifold-guard==0.1.4
```

Observed smoke result:

```text
0.1.4
emit The capital of France is Paris.
```

PyPI install smoke:

```bash
python -m pip install manifold-guard==0.1.4
```

Observed smoke result:

```text
0.1.4
emit The capital of France is Paris.
```

## Release notes

This release adds focused offline support for:

- applicant age-range paraphrases
- conditional scope and all-bad conditional near misses
- nested exception and subgroup exclusion scope
- ordinal, ranked, and stage-slot bindings
- aggregate count-to-group bindings
- actor-bound permission scope

No public API signature changes were introduced.

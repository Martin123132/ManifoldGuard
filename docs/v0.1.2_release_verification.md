# v0.1.2 Release Verification

Release date: 2026-06-19

Package: `manifold-guard==0.1.2`

Tag: `v0.1.2`

## GitHub release path

- Release-prep PR: `#33`
- Release tag build: passed
- TestPyPI trusted publish workflow: passed
- PyPI trusted publish workflow: passed

## Local release-prep validation

- `python scripts\validate_examples.py`: passed
- `python scripts\docs_quality.py`: passed
- `python -m pytest -q`: `174 passed`
- `python -m build --outdir <temp>`: built wheel and source distribution
- Temp wheel install smoke: passed
- Offline CLI JSON smoke from wheel install: passed

## Package-index verification

TestPyPI smoke:

- Fresh virtual environment created.
- Installed `manifold-guard==0.1.2` from TestPyPI with PyPI dependency fallback.
- Imported `mbt_ai_tools`.
- Confirmed package version `0.1.2`.
- Ran offline CLI JSON smoke successfully.

PyPI smoke:

- Fresh virtual environment created.
- Installed `manifold-guard==0.1.2` from PyPI.
- Imported `mbt_ai_tools`.
- Confirmed package version `0.1.2`.
- Ran offline CLI JSON smoke successfully.

## Challenge-corpus status at release

EXP21 challenge corpus result at release: `37 / 37`.

This is a development challenge-corpus pass result, not a universal safety or
fact-checking claim. It records that the current offline reference-bound guards
pass the checked EXP21 seed cases at the time of release.

## Public install command

```bash
python -m pip install manifold-guard==0.1.2
```

Optional embedding mode remains opt-in:

```bash
python -m pip install "manifold-guard[embeddings]==0.1.2"
```
